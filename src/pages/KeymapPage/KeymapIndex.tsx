import { Box } from '@chakra-ui/react'
import { RouteComponentProps } from '@reach/router'
import React from 'react'

const KeymapIndex = (props: RouteComponentProps) => <Box>Nothing to see here !</Box>

export default KeymapIndex
